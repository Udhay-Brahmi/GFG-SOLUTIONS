#include <bits/stdc++.h>
using namespace std;

int main() {
	//code
	int n,a;
	cin>>n;
	while(n--){
	    cin>>a;
	    int arr[a*a];
	    for(int i=0;i<(a*a);i++){
	        cin>>arr[i];
	    }
	    sort(arr,arr+(a*a));
	    for(auto e : arr)cout<<e<<" ";
	    cout<<endl;
	}
	return 0;
}
