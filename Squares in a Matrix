#include <iostream>
using namespace std;

int main() {
	//code
	int n,a,b;cin>>n;
	while(n--){
	    cin>>a>>b;
	    long long int sum=0;
	    while(a!=0 && b!=0){
	        sum+=a*b;
	        a--;b--;
	    }
	    cout<<sum<<endl;
	}
	return 0;
}
