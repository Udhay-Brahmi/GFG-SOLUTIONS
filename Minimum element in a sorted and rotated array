#include <bits/stdc++.h>
using namespace std;

int main() {
	//code
	int n,a,d;cin>>n;
	while(n--){
	    cin>>a;
	    int m=INT_MAX;
	    for(int i=0;i<a;i++){
	        cin>>d;
	        m=min(m,d);
	    }
	    cout<<m<<endl;
	}
	return 0;
}
