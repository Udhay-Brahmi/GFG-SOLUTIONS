#include <bits/stdc++.h>
using namespace std;

int main() {
	int n,a;cin>>n;
	while(n--){
	    cin>>a;
	    int arr[a];
	    for(int i=0;i<a;i++)cin>>arr[i];
	    sort(arr,arr+a);
	    cout<<arr[a-2]<<" "<<arr[1]<<endl;
	}
	return 0;
}
