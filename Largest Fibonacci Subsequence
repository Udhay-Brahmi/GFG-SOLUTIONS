#include <bits/stdc++.h>
using namespace std;

int main() {
	//code
	int n,a,d;
	cin>>n;
	while(n--){
	    cin>>a;
	    for(int i=0;i<a;i++) {
	        cin>>d;
	        if(  floor(sqrt(5*d*d - 4))==ceil(sqrt(5*d*d-4))||floor(sqrt(5*d*d + 4))==ceil(sqrt(5*d*d+4)) ){
	            cout<<d<<" ";
	        }
	    }
	    cout<<endl;
	}
	return 0;
}
